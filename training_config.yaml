# SAE training configuration (Phase 2, Week 5: lock optimal hyperparameters)
model_name: "UBC-NLP/MARBERT"
layer: 6
batch_size: 32
lr: 1.0e-4
warmup_steps: 100
l1_coeff: 1.0e-3
dict_mult: 8
num_steps: 10000
checkpoint_every: 1000
max_grad_norm: 1.0
wandb_project: "sae-arabic"
seed: 42

# Data (Phase 1)
dataset_path: "Abdelrahman-Rezk/Arabic_Dialect_Identification"
dataset_name: null
text_column: "text"
dialect_column: "label"
split: "train"
num_samples: 200
max_length: 128

# Outputs
out_dir: "data/real_run"
