# Copy to .env and fill in. Never commit .env (it is gitignored).

# HuggingFace token (download MARBERT, NADI/MADAR)
HF_TOKEN=

# Weights & Biases API key (experiment tracking)
WANDB_API_KEY=

# Optional: ALDi model identifier / scoring endpoint (Phase 3)
ALDI_MODEL=

# Optional: Kaggle credentials if running from local Kaggle API
KAGGLE_USERNAME=
KAGGLE_KEY=
